spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DRIVER_CLASS_NAME}
  jpa:
    hibernate:
      ddl-auto: ${JPA_GENERATION}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      timeout: 60000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 1
          max-wait: 2000ms
  kafka:
    bootstrap-servers: ${BOOTSTRAP_SERVERS}
    topics:
      shift-opened: ${KAFKA_TOPIC_SHIFT_OPENED}
      shift-closed: ${KAFKA_TOPIC_SHIFT_CLOSED}
      order-closed: ${KAFKA_TOPIC_ORDER_CLOSED}
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: io.apicurio.registry.serde.avro.AvroKafkaDeserializer
      group-id: ${KAFKA_GROUP_ID}
      properties:
        apicurio.registry.url: ${SCHEMA_REGISTRY_URL}
        apicurio.registry.auto-register: true
        apicurio.registry.use-specific-avro-reader: true
        spring:
          json:
            trusted:
              packages: "*"
        auto-offset-reset: earliest

server:
  port: ${SERVER_PORT}

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true # /actuator/health/liveness
    readinessstate:
      enabled: true # /actuator/health/readiness
    db:
      enabled: true